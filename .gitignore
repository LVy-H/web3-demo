# Dependencies
node_modules/
# Hardhat
cache/
artifacts/
typechain-types/
typechain/
# Frontend (Vite/React)
dist/
dist-ssr/
*.local
# Playwright E2E (videos + HTML report — generated demo artifacts)
codes/frontend/test-results/
codes/frontend/playwright-report/
codes/frontend/test-results-metamask/
codes/frontend/playwright-report-metamask/
# Synpress (real-MetaMask E2E) wallet/browser caches
codes/frontend/.cache-synpress/
codes/frontend/.playwright-browsers/
# Environment
.env
.env.local
.env.*.local
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log
# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
# Build output / coverage
coverage/
coverage.json
# Typescript
*.tsbuildinfo
# Nix
result
result-*
# direnv (flake devShell cache)
.direnv/
.envrc
# Local agent / tooling state (Claude Code, skills, scratch — never commit)
.claude/
.agents/
.tmp/
skills-lock.json
