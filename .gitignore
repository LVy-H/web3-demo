# Dependencies
node_modules/
# Local demo stack (demo.sh): built web app served into the site + server state
site/demo/
.demo-data/
.out/
# Hardhat
cache/
artifacts/
typechain-types/
typechain/
# Frontend (Vite/React)
dist/
dist-ssr/
*.local
# Environment
.env
.env.local
.env.*.local
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log
# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
# Build output / coverage
coverage/
coverage.json
# Typescript
*.tsbuildinfo
# Nix
result
result-*
# direnv (flake devShell cache)
.direnv/
.envrc
# Local agent / tooling state (Claude Code, skills, scratch — never commit)
.claude/
.agents/
.tmp/
skills-lock.json
