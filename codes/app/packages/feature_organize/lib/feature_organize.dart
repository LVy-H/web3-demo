/// Tessera organize space (R3). Real screens land via the R3 fan-out;
/// this stub only proves the package wires into the workspace.
library;

const String featureOrganizePackageName = 'feature_organize';
