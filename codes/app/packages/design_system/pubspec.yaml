name: design_system
description: "Dark Bauhaus design system — Db tokens, typography, theme, and shared chrome widgets (dot grid, watermark, results bars)."
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
