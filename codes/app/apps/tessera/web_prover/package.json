{
  "name": "tessera-web-prover",
  "private": true,
  "type": "module",
  "description": "Self-contained Semaphore v4 proof bundle for Tessera's web + desktop ProofService. Build: `npm install && npm run build` -> ../web/zkprover.js. Decoupled from the deprecated React frontend.",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "@semaphore-protocol/group": "^4.14.2",
    "@semaphore-protocol/identity": "^4.14.2",
    "@semaphore-protocol/proof": "^4.14.2"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
